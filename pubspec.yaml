name: drivewise
description: Gamified safe driving app – 2026 MAD Contest (Azure Backend)
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Azure Auth
  aad_oauth: ^1.3.0  # For Azure AD B2C
  # Azure Cosmos DB
  azure_cosmos: ^0.1.0  # Or use http for REST
  http: ^1.2.0
  # Core (unchanged)
  geolocator: ^10.1.0
  sensors_plus: ^4.0.0
  flutter_tts: ^3.8.0
  haptic_feedback: ^0.1.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  shared_preferences: ^2.2.2
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
